const ola = {
  dizOla(){
    return "Olá, fullcycle!";
  },

  soma(primeiroNumero, segundoNumero){
    return primeiroNumero + segundoNumero;
  },

  subtrai(primeiroNumero, segundoNumero){
    return primeiroNumero - segundoNumero;
  },

  multiplica(primeiroNumero, segundoNumero){
    return primeiroNumero * segundoNumero;
  },

  divide(primeiroNumero, segundoNumero){
    return primeiroNumero / segundoNumero;
  },

  potencia(primeiroNumero, segundoNumero){
    return Math.pow(primeiroNumero, segundoNumero);
  },

  raiz(numero){
    return Math.sqrt(numero);
  },

  geraLinhasApenasParaComplementar(){
    let numero = 0;
    numero = numero + 1;
    numero = numero + 1;
    numero = numero + 1;
    numero = numero + 1;
    numero = numero + 1;
    numero = numero + 1;
    numero = numero + 1;
    numero = numero + 1;
    numero = numero + 1;
    numero = numero + 1;
    numero = numero + 1;
    numero = numero + 1;
    numero = numero + 1;
    numero = numero + 1;
    numero = numero + 1;
    numero = numero + 1;
    numero = numero + 1;
    numero = numero + 1;
    numero = numero + 1;
    numero = numero + 1;
    numero = numero + 1;
    numero = numero + 1;
    numero = numero + 1;
    numero = numero + 1;
    numero = numero + 1;
    numero = numero + 1;
    numero = numero + 1;
    numero = numero + 1;
    numero = numero + 1;
    numero = numero + 1;
    numero = numero + 1;
    numero = numero + 1;
    numero = numero + 1;
    numero = numero + 1;
    numero = numero + 1;
    numero = numero + 1;
    numero = numero + 1;
    numero = numero + 1;
    numero = numero + 1;
    numero = numero + 1;
    return numero;
  }
};

module.exports = ola;
